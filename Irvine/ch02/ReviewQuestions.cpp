### 1. 在 32 位模式下，除了栈指针(ESP) 之外，还有哪个寄存器指向栈上的变量？

* *EBP（基址指针寄存器，Extended Base Pointer） * *。在函数调用建立栈帧后，EBP 固定指向当前栈帧的基址，函数通过 `[EBP + 偏移量]` 访问参数，通过 `[EBP - 偏移量]` 访问局部变量。ESP 则随 push / pop 动态移动。

-- -

### 2. 至少说出四个 CPU 状态标志

- **CF（进位标志，Carry Flag） * *：无符号运算溢出 / 借位时置 1
- **ZF（零标志，Zero Flag） * *：运算结果为 0 时置 1
- **SF（符号标志，Sign Flag） * *：运算结果为负时置 1（等于结果最高位）
- **OF（溢出标志，Overflow Flag） * *：有符号运算溢出时置 1
- **PF（奇偶标志，Parity Flag） * *：结果最低字节中 1 的个数为偶数时置 1
- **AF（辅助进位标志，Auxiliary Carry Flag） * *：BCD 运算中低 4 位向高 4 位进位 / 借位时置 1

-- -

### 3. 无符号算术运算结果太大，无法放入目的操作数时，哪个标志被置位？

* *CF（进位标志） * *。例如 `ADD AL, 255` 若 AL = 1，结果 256 超出 8 位，CF = 1。

-- -

### 4. 有符号算术运算结果太大或太小，无法放入目的操作数时，哪个标志被置位？

* *OF（溢出标志） * *。例如 8 位有符号数 `127 + 1 = -128`（补码回绕），此时 OF = 1，表示结果超出了有符号数的表示范围。

-- -

### 5. (判断)当寄存器操作数大小为 32 位且使用 REX 前缀时，R8D 寄存器可供程序使用。

* *True（正确） * *。REX 前缀扩展了寄存器编码，使得 R8D～R15D 这 8 个新增的 32 位通用寄存器可用。

-- -

### 6. 算术或逻辑运算产生负结果时，哪个标志被置位？

* *SF（符号标志） * *。SF 直接等于运算结果的最高位（符号位），结果为负时 SF = 1。

-- -

### 7. CPU 的哪部分执行浮点运算？

* *FPU（浮点运算单元，Floating - Point Unit） * *。在 x86 架构中历史上是独立的 x87 协处理器，现已集成在 CPU 内部；此外 SSE / AVX 等 SIMD 单元也能执行浮点运算。

-- -

### 8. 在 32 位处理器上，每个浮点数据寄存器有多少位？

* *80 位 * *。x87 FPU 的 8 个数据寄存器（ST0～ST7）均为 80 位扩展精度格式（1 位符号 + 15 位指数 + 64 位尾数）。

-- -

### 9. (判断)x86 - 64 指令集向后兼容 x86 指令集。

* *True（正确） * *。x86 - 64（AMD64）是 x86 指令集的超集，处理器可以运行在传统模式（Legacy Mode）执行 32 位 x86 代码，也可以在长模式的兼容子模式下运行 32 位程序。

-- -

### 10. (判断)在当前的 64 位芯片实现中，所有 64 位都用于寻址。

* *False（错误） * *。当前 x86 - 64 处理器实际只使用 * *48 位虚拟地址 * *（线性地址）和 * *40～52 位物理地址 * *，并非全部 64 位。高位被要求做符号扩展，未使用的位保留给未来扩展。

-- -

### 11. (判断)Itanium 指令集与 x86 指令集完全不同。

* *True（正确） * *。Itanium（安腾）采用 IA - 64 架构，基于 EPIC（显式并行指令计算） / VLIW 设计，指令格式、执行模型、寄存器架构都与 x86 CISC 架构完全不同。

-- -

### 12. (判断)静态 RAM 通常比动态 RAM 便宜。

* *False（错误） * *。SRAM 每位需要约 6 个晶体管，而 DRAM 每位只需 1 个晶体管 + 1 个电容，因此 * *SRAM 比 DRAM 贵得多 * *。SRAM 速度快但成本高，通常用作 CPU 缓存；DRAM 容量大、成本低，用作主存。

-- -

### 13. (判断)使用 REX 前缀时，64 位 RDI 寄存器可用。

* *True（正确） * *。REX.W 位（REX 前缀的第 4 位）将操作数大小提升为 64 位，此时 RDI 等 64 位寄存器均可使用。

-- -

### 14. (判断)在原生 64 位模式下，可以使用 16 位实模式，但不能使用虚拟 8086 模式。

* *False（错误） * *。在 64 位长模式（Long Mode）下， * *实模式和虚拟 8086 模式都不可用 * *。处理器要运行实模式代码必须切换回传统模式（Legacy Mode）。兼容子模式（Compatibility Mode）支持 16 / 32 位保护模式代码，但不支持实模式和 V86 模式。

-- -

### 15. (判断)x86 - 64 处理器比 x86 处理器多 4 个通用寄存器。

* *False（错误） * *。x86 有 8 个通用寄存器（EAX / EBX / ECX / EDX / ESI / EDI / EBP / ESP），x86 - 64 有 16 个（RAX～R15）， * *多了 8 个 * *，不是 4 个。

-- -

### 16. (判断)64 位版本的 Microsoft Windows 不支持虚拟 8086 模式。

* *True（正确） * *。64 位 Windows 完全取消了对 16 位程序和虚拟 8086（V86）模式的支持，因为 x86 - 64 长模式下 V86 模式不存在。

-- -

### 17. (判断)DRAM 只能用紫外线擦除。

* *False（错误） * *。用紫外线擦除是 * *EPROM * *（可擦除可编程只读存储器）的特性。DRAM（动态随机存取存储器）是可读写的，通过电容充放电存储数据，需要定期刷新，与紫外线擦除毫无关系。

-- -

### 18. (判断)在 64 位模式下，最多可以使用 8 个浮点寄存器。

* *False（错误） * *。64 位模式下 SSE / AVX 提供了 * *16 个 XMM 寄存器 * *（XMM0～XMM15），AVX - 512 更扩展到 32 个 ZMM 寄存器。即使是 x87 FPU 也有 8 个 ST 寄存器，但 64 位模式下主要使用 SSE / AVX，浮点寄存器数量远多于 8 个。

-- -

### 19. (判断)总线是一根塑料电缆，两端连接主板，但不直接放在主板上。

* *False（错误） * *。总线（Bus）是 * *主板上的一组平行导线 / 印刷线路 * *，用于在 CPU、内存和 I / O 设备之间传输地址、数据和控制信号，不是外部电缆。

-- -

### 20. (判断)CMOS RAM 与静态 RAM 相同，意味着它不需要额外电源或刷新周期就能保持其值。

* *False（错误） * *。CMOS RAM 虽然功耗极低（采用 CMOS 工艺），但 * *仍需要纽扣电池供电 * *才能保持数据（如 BIOS 设置）。断电后数据会丢失，这与真正不需要刷新的 SRAM 不同——SRAM 只要不断电就保持数据，但 CMOS RAM 本质上是一种低功耗 RAM，仍依赖电池。

-- -

### 21. (判断)PCI 连接器用于显卡和声卡。

* *True（正确） * *。PCI（Peripheral Component Interconnect）总线插槽广泛用于连接各种扩展卡，包括声卡、网卡、老式显卡等。虽然现代显卡更多使用 PCIe，但 PCI 确实可以用于这些设备。

-- -

### 22. (判断)8259A 是一个处理来自硬件设备的外部中断的控制器接口。

* *True（正确） * *。8259A 是 * *可编程中断控制器（PIC） * *，负责管理和优先级仲裁来自硬件设备的外部中断请求，将其转发给 CPU。现代系统中仍以兼容形式存在（或集成在 APIC 中）。

-- -

### 23. 汇编语言程序可以在哪一级别操纵输入 / 输出？

汇编语言可以在 * *多个层级 * *操纵 I / O：

1. * *最底层——硬件端口级 * *：直接使用 `IN`/`OUT` 指令读写 I / O 端口，直接与硬件寄存器交互（在保护模式下需要 I / O 权限）。
2. * *操作系统 API 级 * *：通过操作系统提供的系统调用（如 32 位下的 `INT 21h`/`INT 80h`，64 位下的 `syscall`/`sysenter`）请求内核完成 I / O 操作。
3. * *BIOS / 固件级 * *：在实模式下通过 BIOS 中断（如 `INT 10h` 视频、`INT 13h` 磁盘）进行底层 I / O。

汇编语言的核心优势就是 * *可以直达硬件层 * *，这是高级语言做不到的。

-- -

### 24. 为什么游戏程序经常直接将声音输出发送到声卡的硬件端口？

主要原因是 * *追求极致的低延迟和精确的时序控制 * *：

1. * *降低延迟 * *：直接写硬件端口绕过了操作系统的音频抽象层（如 Windows 的 MME / WASAPI 等），减少了缓冲区排队和内核态切换的开销，音效响应更及时。
2. * *精确时序控制 * *：游戏中的音效（如枪声、脚步声、碰撞音）需要与画面帧严格同步。直接操作硬件可以精确控制播放时机，避免系统音频栈的不可预测延迟。
3. * *性能优化 * *：在早期 CPU 性能有限的年代，直接操作硬件可以减少 CPU 占用，把更多资源留给图形渲染和游戏逻辑。
4. * *定制化混音 * *：游戏可以自己实现高效的软件混音器，直接将混合后的 PCM 数据写入声卡缓冲区，完全掌控音频处理流程。

> 不过在现代操作系统中，由于保护模式的安全限制（I / O 权限隔离），用户态程序通常无法直接访问硬件端口，而是通过 WASAPI Exclusive Mode、ASIO 等低延迟音频 API 来近似实现类似效果。